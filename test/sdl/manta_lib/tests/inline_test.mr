private import "../lib/atomic_types.mr" as types

private node InlineTest {
    input vector_input;
    default output reverse_vector: 
        types::vector(
            vector_input.a, 
            vector_input.b, 
            vector_input.g, 
            vector_input.r
        );
}

private node Container {
    input vector_input;
    default output reverse_vector:
        InlineTest(vector_input);
}

Container test(
    types::vector(1.0, 2.0, 3.0, 4.0)
)
