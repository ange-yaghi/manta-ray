node Add: (
    input A,
    input B,
    default output C: A + B
)

node SampleNode: (
    input A,
    input B,
    default output C: add_mod // This should take the default output 'C'
) {
    Add add_mod(A, B)
}
