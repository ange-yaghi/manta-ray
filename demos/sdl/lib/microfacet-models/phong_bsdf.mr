private import "microfacet_refl_bsdf.mr" as microfacet

public node PhongDistribution => __builtin__PhongDistribution {
    input power: 32.0;
}

public node PhongBsdf {
    input power: 32.0;
    default output bsdf: 
        MicrofacetReflBsdf(
            PhongDistribution(power)
        );
}
